//according to the platform that the user is using the application, the url will change accordingly,
//this url is the begining or the url that we talk to the server with it.

// import { Platform } from "react-native";

// let baseURL = "";

// {
//     Platform.OS == "android" ? (baseURL = "http://10.0.2.2:3000/api/v1/") : (baseURL = "http://localhost:3000/api/v1/");
// }

let baseURL = "http://20.234.29.239/node/api/v1/";

export default baseURL;
